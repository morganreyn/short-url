extends layout

block content
  if data
    p Created!
    p #{data.url} --> #{data.redirect}
  form(method="post")
    input#create-input(type="text", name="create", placeholder="https://")
    input(type="submit", value="Create")
  hr
  a(href="/admin") Admin Area